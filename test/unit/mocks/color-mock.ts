/**
 * The Color Mock
 */
export class Color {
  cyan(output) {
    return `cyan(${output})`;
  }

  red(output) {
    return `red(${output})`;
  }

  green(output) {
    return `green(${output})`;
  }
}
